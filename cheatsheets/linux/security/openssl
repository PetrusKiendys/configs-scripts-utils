# Use the OpenSSL cryptographic toolkit

# view SSL/TLS handshake when connecting to a server as a client using OpenSSL s_client,
# example included
openssl s_client -connect <host>:<port>
openssl s_client -connect www.google.com:443
